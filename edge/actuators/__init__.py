"""Optional edge-tier actuator drivers (GPIO): light, spare relay.

Actuators are optional and pluggable — the core system is fully useful with none
installed (see docs/ARCHITECTURE.md, "Actuators are optional and pluggable").
"""
