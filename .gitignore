# Per-user Claude Code settings — local, not shared
.claude/settings.local.json

# Python
__pycache__/
*.py[cod]
.venv/
.venv-compute/
venv/
*.egg-info/
.pytest_cache/

# OS
.DS_Store

# Playwright MCP scratch (snapshots/screenshots/console logs from UI verification)
.playwright-mcp/

# Edge runtime settings (per docs/ARCHITECTURE.md — not committed)
edge/config/settings.json

# Model artifacts and captured media are not committed (see docs/ARCHITECTURE.md).
# Add scoped ignores under compute/models and compute/dataset once they hold artifacts.
/data/
# YOLO/ultralytics downloads weights into the CWD (repo root) on first detector run.
*.pt
