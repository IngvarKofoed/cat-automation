# Runtime deps for the compute ingest client. Test-only deps (pytest, and
# werkzeug for the in-process edge integration test) are intentionally omitted,
# matching edge/requirements.txt's runtime-only convention; the dev venv installs
# them separately.
requests>=2.31
# StreamFrame.image decodes JPEG bodies via cv2+numpy, imported lazily only on
# that path — but they ARE needed to run that shipped code path, so they belong
# here. Headless build matches the edge and is enough for imdecode.
numpy
opencv-python-headless
# Frame-collection browser: FastAPI app + ASGI server. [standard] pulls uvicorn's
# httptools/websockets extras so the served page and media perform well.
fastapi
uvicorn[standard]
# Async HTTP client for the frontend dev proxy (compute/tools/frontend_dev_proxy.py):
# it streams responses (incl. the long-lived SSE feed) without blocking the event loop,
# where a blocking client would freeze the proxy. Dev-only; usually already present as
# a transitive dep, but declared so a fresh .venv-compute has it.
httpx
